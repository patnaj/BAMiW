@{
    ViewData["Title"] = "Error details";
}

<h2>Error details</h2>

@if (ViewBag.Path != null)
{
    <p>Path: @ViewBag.Path</p>
}

@if (ViewBag.Error != null)
{
    <pre>@ViewBag.Error</pre>  <!-- ujawnia pełny wyjątek -->
}
